# <Company> brainstorms

This company's exploratory thinking and proposals, not yet decided. When one
resolves, record it in `../decisions/` and fold the outcome into `../knowledge/`.
Start from `templates/brainstorm.md`.
