# <Company> — code

This company's single project. The `code/` folder *is* the project (we keep one
project per company; stay flexible). Add its own `CLAUDE.md` here once the
codebase has enough technical context to warrant one.
